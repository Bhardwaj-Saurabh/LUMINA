{
  "$comment": "Case law for the 2026-03 cohort, public copy shipped with LUMINA. Append only. Never renumber. Every rule must cite a real precedent; placeholders are marked TODO and reported by P2, which warns until the cohort fills them. That nag is intentional: a rule without an incident behind it is a preference, not case law. When a review catches something new, add the precedent.",
  "version": "0.1",
  "rules": [
    {
      "id": "C1",
      "title": "Declared expectations are internally coherent",
      "type": "contract",
      "severity": "error",
      "rule": "Every budget is a positive number, every ratio threshold is within 0 to 1, and every declared file path exists.",
      "precedent": [
        "2026-09-13 LUMINA: the gateway's declared rate limit (30 req/min flat) was incompatible with the declared benchmark workload (40 web + 30 doc queries at concurrency 4, plus document-status polling every 1.2 s). The first full recall@5 run collected 12 x 429 before anyone noticed the two declarations could not both hold; the limiter was re-declared weighted (answer 5, poll 1) at 600/min. Earlier the same week an impossible AZURE_OPENAI_API_VERSION=2026-03-17 in .env made every provider call 404 and wasted the first live run (runs/failing/req_mtxxvg54uhxigd.json)."
      ],
      "selfCheck": "Could this expectations file pass under any possible run? If a threshold is above 1.0 or a path is missing, no.",
      "params": {}
    },
    {
      "id": "A1",
      "title": "Tool errors surface as errors",
      "type": "trajectory",
      "severity": "error",
      "rule": "Every tool call recorded with ok=false must carry a non-empty error string, so a failure is distinguishable from an empty result in the trajectory.",
      "precedent": [
        "FDE Assignment 1, Live Translate (cohort 2026-01): a dependency mismatch made every LLM call throw. The handler wrapped the call in a try/except that returned the input text untouched, so the \"translator\" served English to every user and every response looked like a success: 200, a body, no error in the log. It shipped that way for weeks and was found by a human reading output, not by a test. The rule is the fix: a failed call must be distinguishable from an empty result, which means ok=false AND a non-empty error string, and a provider exception must reach the caller as a 502."
      ],
      "selfCheck": "Force a tool to fail. Read the transcript. Can you tell it failed rather than returned nothing?",
      "params": {}
    },
    {
      "id": "A2",
      "title": "The loop terminates because it finished",
      "type": "trajectory",
      "severity": "error",
      "rule": "Every run ends with terminated='done'. A run ending at an iteration or token cap is a failure even when the final answer is right.",
      "precedent": [
        "2026-09-13 LUMINA: runs/failing/req_d3fb21d7-6bd.json — web_search hung 229.5 s when the Mongo pool reset; the request streamed its first token 230 s into a 90 s quick gear and finished with a plausible answer. It self-reported terminated='cap' and sat in runs/ failing A2 until it was moved to runs/failing/. The M2 fix had bounded LLM turns only; tool dispatch was still unbounded (also runs/failing/req_mtxy8h693vl8og.json, a 295 s provider turn)."
      ],
      "selfCheck": "Why did this loop stop? If the answer is 'it ran out', the agent did not work.",
      "params": {}
    },
    {
      "id": "A3",
      "title": "No tool thrash",
      "type": "trajectory",
      "severity": "warn",
      "rule": "No tool is called more than maxConsecutiveSameTool times in a row without an intervening different call.",
      "precedent": [
        "2026-09-12 LUMINA M2: tool schemas were advertised to the model as placeholder `{type:'object'}`, so it guessed argument names and produced two failed web_search calls per run before landing one that parsed — budget burned, no evidence gained. Fixed by advertising real JSON schemas (inputJsonSchema on every ToolDef)."
      ],
      "selfCheck": "Scan the call sequence. Is the same tool repeating because it is making progress, or because it is stuck?",
      "params": {
        "maxConsecutiveSameTool": 3
      }
    },
    {
      "id": "R1",
      "title": "Required tools were actually called",
      "type": "trajectory",
      "severity": "error",
      "rule": "Every tool named in trajectory.mustCallTools appears at least once in the run.",
      "precedent": [
        "2026-09-13 LUMINA M7: asked gold question g28 with an indexed Space attached, the model called recall_memory and then answered from its own weights — sources: [], a confident ungrounded answer, retrievalRate < 1. Optimistic streaming had already put the first token on the wire, so it could not be caught after the fact; retrieval is now forced in the request (tool_choice 'required' until a retrieval tool has run — core/loop.ts)."
      ],
      "selfCheck": "Did retrieval actually happen, or did the model already know the answer and skip it?",
      "params": {}
    },
    {
      "id": "R2",
      "title": "Forbidden tools were not called",
      "type": "trajectory",
      "severity": "error",
      "rule": "No tool named in trajectory.mustNotCallTools appears in the run.",
      "precedent": [
        "2026-09-13 LUMINA M7: with every tool advertised and tool_choice 'required', the model satisfied the requirement with the cheapest tool it had (recall_memory) instead of a retrieval tool, and recall@5 FELL from 0.974 to 0.897. Controlling which tools may be called is a property of the advertised set, not the prompt: a turn that dodges retrieval now gets the set narrowed to retrieval tools only. Same lesson as plan_research being removed from the quick registry at dispatch (core/registry.ts), not by instruction."
      ],
      "selfCheck": "Could this run have reached anything outside the declared corpus?",
      "params": {}
    },
    {
      "id": "E1",
      "title": "A gold set exists and is non-trivial",
      "type": "eval",
      "severity": "error",
      "rule": "The declared goldSetPath exists and holds at least minGoldItems examples.",
      "precedent": [
        "2026-09-13 LUMINA: `bench --smoke` scores recall@5 over the first THREE gold questions and reported 3/3 — a pass that proves almost nothing. The full 39-question gold set was run separately (39/39 = 1.000) before the RAG milestone was marked done; PROGRESS.md records both numbers side by side so the small sample is never mistaken for the gate."
      ],
      "selfCheck": "How many items is this score computed over? Under 30, it is an anecdote.",
      "params": {
        "minGoldItems": 30
      }
    },
    {
      "id": "E2",
      "title": "Declared eval thresholds are met",
      "type": "eval",
      "severity": "error",
      "rule": "Every threshold declared under eval is met or exceeded by the matching metric in the eval report.",
      "precedent": [
        "2026-09-13 LUMINA: the first completing smoke bench missed exactly one target — ttft p95 3060 ms vs 2500 — and was recorded as FAILED in PROGRESS.md rather than the target being touched. Closed 2026-09-14 by instrumenting per-turn timings and removing three costs (recall offered to memory-less users, web search serial after turn 1, a 500-char snippet that made the model fetch pages): 2354 ms on the same gate."
      ],
      "selfCheck": "Was this threshold set before or after seeing the score?",
      "params": {}
    },
    {
      "id": "E3",
      "title": "A judge model may report, never block",
      "type": "manual",
      "severity": "warn",
      "rule": "No rule with severity=error may derive its verdict from a model's opinion. Subjective metrics are recorded at warn and never gate a merge.",
      "precedent": [
        "Ported precedent, hyperframes-student-kit rule Q1 (2026-09-03): the requirement was that a presenter's camera tile never be cropped. Three colour-threshold detectors were built to find the face in the rendered frame. All three returned confident wrong answers by locking onto slide art and wallpaper, and cropped output shipped twice. The fix was to stop detecting and assert arithmetic against numbers declared in advance. LLM-as-judge is the same failed detector wearing a friendlier hat."
      ],
      "selfCheck": "For each error-severity rule: what produces the verdict? If a model does, demote it to warn or replace it with arithmetic.",
      "params": {}
    },
    {
      "id": "B1",
      "title": "Token budget respected",
      "type": "budget",
      "severity": "error",
      "rule": "Total tokens for a run do not exceed budget.maxTokensPerRun.",
      "precedent": [
        "2026-09-13 LUMINA M7: search_documents retrieved topK=8 chunks (~1200 chars each) into the answer prompt while the grader scores only the top 5 — chunks 6–8 were input tokens nobody scored, paid on every document answer. Found in the cost line ($0.00128 → $0.00110 per answer) and TTFT, not by any test; RAG_TOP_K is now 5 and a config value."
      ],
      "selfCheck": "Does token use grow with conversation length in a way nobody declared?",
      "params": {}
    },
    {
      "id": "B2",
      "title": "Latency budget respected",
      "type": "budget",
      "severity": "warn",
      "rule": "Wall-clock seconds for a run do not exceed budget.maxWallClockSec.",
      "precedent": [
        "2026-09-12 LUMINA M2: the first live answers were correct, fully cited, and took 8.2 s to the first token against a 2.5 s SLA — three provider round trips, one of which generated the whole answer, discarded it, and generated it again for streaming. The streaming-turn refactor (the answer IS a turn) took it to 4 s; the search cache to ~1.1 s on repeats."
      ],
      "selfCheck": "Would a user wait this long without assuming it broke?",
      "params": {}
    },
    {
      "id": "B3",
      "title": "Cost budget respected",
      "type": "budget",
      "severity": "warn",
      "rule": "Cost in USD for a run does not exceed budget.maxCostUsd.",
      "precedent": [
        "2026-09-14 LUMINA M9: every cost figure to that point ($0.0011/quick, $114/month projected) rested on the scaffold's placeholder Anthropic rates. Azure list prices for gpt-5.4-mini are $0.75/$4.50 per MTok in/out — roughly 2.5–3x — so sla.json cost_model and env.ts were re-declared before any dollar figure was trusted or a deploy priced."
      ],
      "selfCheck": "Multiply this by expected daily volume. Is the business still viable?",
      "params": {}
    },
    {
      "id": "P1",
      "title": "A human read a full trajectory",
      "type": "manual",
      "severity": "info",
      "rule": "Someone read one complete successful trajectory and one complete failing trajectory, every step, before sign-off.",
      "precedent": [
        "Ported precedent, hyperframes-student-kit rule P3: lint passing never once caught a cropped face, a caption landing on the wrong word, or text overflowing a box. Only loading the frames and looking at them did."
      ],
      "selfCheck": "Name the two runs you read. If you cannot produce a failing one, you do not understand the failure surface yet.",
      "params": {}
    },
    {
      "id": "P2",
      "title": "Every rule cites a real precedent",
      "type": "meta",
      "severity": "warn",
      "rule": "No rule in this file carries a precedent beginning with TODO. A rule without a real incident is a preference, not case law.",
      "precedent": [
        "Ported precedent, hyperframes-student-kit addons/README.md: 'When a review catches something new, add the precedent \u2014 an empty lessons file is the failure mode this exists to prevent.'"
      ],
      "selfCheck": "Can you name the incident, the date, and what it cost? If not, delete the rule or go find the story.",
      "params": {}
    }
  ]
}
